// import { createContext } from "react";

// export const CartProvider = createContext();

// export const Createcartprovider = ({ children }) => {
//   <CartProvider.Provider value={10}> {children} </CartProvider.Provider>;
// };
